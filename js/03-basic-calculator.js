// ADD A FUNCTION CALLED CALCULATE
function calculate(){
    let num1 = parseInt(prompt("Enter a number to calculate"))
    let num2 = parseInt(prompt("Enter a second number to calculate"))
    let operation = prompt("Enter the mathmatical operation you want to  perform: (+,-,*,/)")

}
// COLLECT FIRST NUMBER FROM USER

// COLLECT SECOND NUMBER FROM USER

// COLLECT OPERATION TO PERFORM (+,-,*,/) FROM USER

// CALL THE FUNCTION AND RETURN THE RESULT WITHIN AN ALERT
